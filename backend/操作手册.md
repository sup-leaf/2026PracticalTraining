# 校园集市 - 后端操作手册

## 一、环境要求

### 1. 开发环境
- JDK 17+
- Maven 3.6+
- MySQL 8.0
- Redis（可选）

### 2. 端口占用
- 8080：后端服务
- 3306：MySQL（默认）
- 6379：Redis（默认）

---

## 二、配置说明

### 1. 数据库配置
文件：`src/main/resources/application.yml`

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campus_market?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 你的MySQL密码  # 替换为你的MySQL密码

  redis:
    host: localhost
    port: 6379
```

### 2. 上传配置
```yaml
upload:
  path: D:/dasanxiaShixun/project/backend/uploads
```

### 3. 修改密码步骤
1. 打开 `application.yml`
2. 修改 `spring.datasource.password` 为你的MySQL密码
3. 保存文件

---

## 三、数据库初始化

### 1. 创建数据库
```sql
CREATE DATABASE IF NOT EXISTS campus_market DEFAULT CHARACTER SET utf8mb4;
USE campus_market;
```

### 2. 创建表（按顺序执行）
```sql
-- 用户表
CREATE TABLE t_user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(100) NOT NULL,
    user_type TINYINT NOT NULL COMMENT '1-学生 2-企业 3-教师',
    real_name VARCHAR(50),
    student_id VARCHAR(20),
    campus_card_no VARCHAR(20),
    company_name VARCHAR(100),
    company_code VARCHAR(20),
    teacher_no VARCHAR(20),
    phone VARCHAR(20),
    email VARCHAR(50),
    status TINYINT DEFAULT 1,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 岗位表
CREATE TABLE t_job (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(100) NOT NULL,
    description TEXT,
    requirement TEXT,
    skill_tags VARCHAR(200),
    job_type TINYINT NOT NULL COMMENT '1-实习 2-全职 3-科研助理',
    location VARCHAR(100),
    salary_min DECIMAL(10,2),
    salary_max DECIMAL(10,2),
    duration INT,
    publisher_id BIGINT NOT NULL,
    publisher_type TINYINT NOT NULL COMMENT '1-企业 2-教师',
    status TINYINT DEFAULT 1,
    view_count INT DEFAULT 0,
    delivery_count INT DEFAULT 0,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 简历表
CREATE TABLE t_resume (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    name VARCHAR(50),
    gender VARCHAR(10),
    age INT,
    phone VARCHAR(20),
    email VARCHAR(50),
    avatar VARCHAR(255),
    major VARCHAR(100),
    grade VARCHAR(50),
    gpa DOUBLE,
    skills TEXT,
    projects TEXT,
    awards TEXT,
    experience TEXT,
    self_evaluation TEXT,
    file_url VARCHAR(255),
    status TINYINT DEFAULT 0,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_id (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 投递记录表
CREATE TABLE t_delivery (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    job_id BIGINT NOT NULL,
    resume_id BIGINT NOT NULL,
    job_publisher_id BIGINT NOT NULL,
    status TINYINT DEFAULT 0 COMMENT '0-待查看 1-已查看 2-面试中 3-已录用 4-已拒绝',
    hr_note VARCHAR(500),
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 四、启动步骤

### 1. 启动前检查
- [ ] MySQL 服务已启动
- [ ] 数据库已创建
- [ ] 所有表已创建
- [ ] 配置文件密码已修改

### 2. 启动命令
```bash
cd D:\dasanxiaShixun\project\backend
mvn spring-boot:run
```

### 3. 启动成功标志
```
Tomcat started on port(s): 8080 (http)
Started MarketApplication in x.xxx seconds
```

### 4. 验证启动
浏览器访问：`http://localhost:8080`

---

## 五、常见问题

### 1. 端口被占用
**问题**：8080 端口被其他程序占用

**解决**：
```powershell
# 查找占用端口的进程
netstat -ano | findstr 8080

# 结束进程（替换为PID）
taskkill /PID <PID> /F
```

### 2. 数据库连接失败
**问题**：无法连接 MySQL

**解决**：
1. 检查 MySQL 服务是否启动
2. 检查 `application.yml` 中的用户名密码
3. 确认数据库已创建

### 3. Maven 下载依赖慢
**解决**：配置阿里云镜像仓库

### 4. 项目启动后请求报错
**解决**：先停止旧进程，再重新启动
```powershell
Get-Process -Name java -ErrorAction SilentlyContinue | Stop-Process -Force
mvn spring-boot:run
```

---

## 六、接口测试示例

### 1. 学生注册
```json
POST /api/auth/register
{
    "username": "student1",
    "password": "123456",
    "userType": 1,
    "realName": "张三",
    "studentId": "23301130",
    "campusCardNo": "123456"
}
```

### 2. 学生登录
```json
POST /api/auth/login
{
    "username": "student1",
    "password": "123456",
    "userType": 1
}
```

### 3. 企业发布岗位
```json
POST /api/job/publish?publisherId=2
{
    "title": "前端开发实习",
    "description": "负责前端开发",
    "requirement": "熟悉Vue",
    "skillTags": "Vue,JavaScript",
    "jobType": 1,
    "location": "北京",
    "salaryMin": 200,
    "salaryMax": 300,
    "publisherType": 1
}
```

### 4. 学生投递
```
POST /api/delivery/apply?jobId=1&resumeId=1
```

### 5. 企业处理投递
```
PUT /api/delivery/status?deliveryId=1&publisherId=1&deliveryStatus=2&note=请参加面试
```

---

## 七、注意事项

1. **密码加密**：用户密码使用 MD5 加密存储
2. **上传文件**：首次使用前需创建 `uploads` 目录
3. **参数编码**：URL 参数避免复制粘贴，建议手动输入
4. **状态码含义**：
   - 投递状态：0-待查看, 1-已查看, 2-面试中, 3-已录用, 4-已拒绝
   - 用户类型：1-学生, 2-企业, 3-教师
   - 岗位类型：1-实习, 2-全职, 3-科研助理

---

## 八、项目成员

| 角色 | 姓名 | 学号 |
|------|------|------|
| 组长 | 马勇 | 23301130 |
| 组员 | 薛探昕 | 23301141 |
| 组员 | 黄冠宇 | 23301124 |
| 组员 | 徐健智 | 23301139 |
| 组员 | 郭书豪 | 23301149 |